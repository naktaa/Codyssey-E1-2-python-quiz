# 진행 상태

## 현재 단계

- 단계: 7 완료 — 카테고리별 퀴즈 목록 조회
- 현재 브랜치: `main` (`origin/main` 추적)
- 프로그램 구현: 카테고리별 플레이·목록, 메모리 기반 추가와 안전 종료 완료
- 실행 검증: 기존 Python 3.12.13 자동 테스트 17개와 전체 컴파일 통과,
  `main.py` 직접 실행으로 정상 종료·EOF·Ctrl+C 종료 확인 완료
- 확인된 오류: EOF traceback 해결, 현재 다음 작업을 막는 실행 오류 없음
- 증거 수집: 퀴즈 목록 캡처, 안전 종료 로그와 예비 화면 확보
- README 최종 반영: 현재 구현 상태 반영, 최종 제출 내용과 화면은 미완료

## 작업 시작 시 Git 기준 상태

- 작업 트리: 깨끗함
- 커밋 수: 3개
- 최근 커밋: `51b89d6 Docs: 초기 Git 설정과 실행 증거 기록`
- 로컬 Git 사용자 이름·이메일: 저장소 범위 설정 확인 완료

## 현재 Git 상태

- 작업 트리: 목록 기능 기록 문서가 미커밋 상태
- 커밋 수: 16개
- `main` 최근 커밋: `5d08e39 Feat: 퀴즈 목록 조회 기능 구현`
- `main`과 로컬 `origin/main`: `5d08e39`에서 같음
- `feature/solving`: `cb4f9cb`에서 `--no-ff` 병합 완료
- `feature/state-json`: `b4cc983` 커밋을 원격에 push하고 별도 보관

## 현재까지 완료한 작업

- 미션 목적과 필수 산출물 정리
- 요구사항 ID와 추적표 작성
- 권장 프로젝트 구조 작성
- 단계별 구현·Git·증거 계획 작성
- 초기 README 템플릿 작성
- Python 3.12.13과 Git 2.53.0 확인
- 로컬 Git 저장소와 `main` 브랜치 초기화
- 초기 커밋 `a4887e4 Chore: 프로젝트 초기 파일 구성` 생성
- GitHub 원격 저장소 연결 및 `origin/main` 첫 push
- 초기 환경·Git 증거 `evidence/git/git-log.png` 확보
- 초기 Git 명령과 실제 결과를 `evidence/git/git-verification.md`에 기록
- 퀴즈 주제를 여러 카테고리로 구성된 상식 퀴즈로 확정
- 카테고리별 플레이와 카테고리별 100점 환산 최고 점수 방식 확정
- 손상된 `state.json`을 백업한 뒤 기본 데이터로 복구하는 방식 확정
- 전체 아키텍처와 단계별 커밋 계획을 `docs/architecture-plan.md`에 정리
- `main.py` 진입점과 `src/game_manager.py`의 최소 `QuizGame` 클래스 구현
- 확장할 기능 코드는 `src/`, 자동 테스트는 `tests/`에 두는 구조 적용
- 메뉴 1~5 표시와 미구현 기능의 메뉴 복귀 처리
- 공백·빈 값·문자·범위 오류를 처리하는 `read_int()` 구현
- 비어 있지 않은 문자열을 받는 `read_nonempty()` 구현
- 메뉴와 공통 입력 자동 테스트 4개 통과
- `Quiz` 클래스의 필드·문자열 정리·형식 검증 구현
- 문제·선택지 출력과 사용자 답의 정답 확인 메서드 구현
- JSON 저장용 딕셔너리 변환과 복원 메서드 구현
- 실제 문제 반영 전 사용할 빈 기본 퀴즈 제공 함수 구현
- `feature/solving` 브랜치 생성과 checkout 기록 확인
- 퀴즈 데이터에서 중복 없는 카테고리를 등장 순서대로 추출
- 선택한 카테고리 문제만 저장된 순서대로 출제
- 각 답의 정오답과 실제 정답 선택지 안내
- 플레이 종료 후 정답 수와 100점 환산 점수 출력
- 퀴즈가 없을 때 안내 후 메뉴 복귀
- `main.py`에서 기본 퀴즈 생성 함수를 호출하도록 연결
- 과학·역사 카테고리의 플레이 확인용 임시 문제 4개 추가
- 카테고리 이름·문제·선택지 4개·정답을 입력하는 퀴즈 추가 구현
- 추가한 퀴즈를 현재 실행 중인 목록에만 반영하도록 JSON 저장과 분리
- 사용자 터미널에서 정답 번호 6을 거부하고 정상 번호를 다시 받는 흐름 확인
- 퀴즈 추가 후 종료·재실행하면 추가 문제가 사라지는 메모리 동작 확인
- `evidence/logs/memory-persistence-test.md`에 실제 비교 로그 기록
- `feature/solving`을 `main`에 병합하고 병합 그래프 확인
- `main` 기준 의미 있는 커밋 13개 확인
- 메모리 기반 퀴즈 추가를 `b78bcc1`로 commit하고 `origin/main`에 push
- Python 3.12.13에서 자동 테스트 17개와 전체 컴파일 통과
- 추가한 새 카테고리를 같은 실행에서 선택해 100점 플레이 결과 확인
- `run()`에서 `EOFError`와 `KeyboardInterrupt`를 함께 처리하도록 구현
- 정상 종료와 입력 중단 안내를 `safe_exit()` 메서드로 분리
- `main.py` 직접 실행에서 메뉴 5번, EOF와 실제 Ctrl+C가 종료 코드 0으로
  끝나고 traceback이 출력되지 않는 것을 확인
- 퀴즈를 카테고리별로 묶고 문제와 선택지를 한 줄로 출력하는 목록 구현
- 문제 사이 한 줄 간격과 목록 확인 후 메인 메뉴 복귀 확인
- `evidence/screenshots/quiz-list.png` 목록 실행 화면 확보

## 확정된 설계

- 카테고리는 각 퀴즈의 `category` 값에서 자동 생성
- 플레이 전 한 카테고리를 선택하고 해당 문제를 저장 순서대로 출제
- 최고 점수는 카테고리별 0~100점으로 `best_scores`에 저장
- 최종 문제를 받기 전까지 임시 문제 4개와 테스트 fixture로 기능 검증

## 나중에 필요한 사용자 데이터

- 실제 상식 퀴즈 카테고리
- 직접 작성한 기본 문제와 정답 최소 5개

## 다음 권장 작업 하나

메뉴 4번의 카테고리별 최고 점수 기능을 구현한다.

## 다음 단계 정상 완료 기준

- 플레이 전에는 각 카테고리를 `기록 없음`으로 표시함
- 플레이 점수를 카테고리별 최고 점수와 비교해 높은 점수만 갱신함
- 메뉴 4번에서 카테고리별 최고 점수를 확인하고 메뉴로 복귀함
- 같은 실행에서 낮거나 같은 점수로 다시 플레이해도 최고 기록을 유지함
- 아직 미구현인 JSON 저장 기능을 함께 변경하지 않음

## 차단 요소

- 최고 점수 구현을 막는 요소는 없다.
- JSON 저장 코드는 `feature/state-json`에 분리되어 있으나 불러오기·손상 복구가
  없고 현재 `main`과 충돌 가능성이 있어 그대로 병합하지 않는다.
- 실제 기본 퀴즈 완성 전에는 사용자 문제 데이터가 필요하다.
- 예비 PNG 증거의 로컬 계정명·호스트명과 Git 이력의 이메일은 공개 전
  최종 점검이 필요하다.
