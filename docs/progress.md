# 진행 상태

## 현재 단계

- 브랜치: `main`
- 현재 기준 커밋: `462a29c Refactor: 상태 관리 책임 분리 및 문서 구조 개선`
- 상태 관리 리팩토링은 `main`과 `origin/main`에 commit·push 완료
- 현재 작업: README 정보 순서와 전체 게임 흐름 단순화
- 상태: README와 진행·작업 기록 수정 및 문서 검증 완료, commit 대기
- README 순서를 게임 소개→구조→객체 역할→실행·기능→전체 흐름으로 재구성
- 복잡한 기능·상태 분기 다이어그램을 실행·복원·메뉴·기능·저장·종료의 단일
  Mermaid 흐름으로 단순화
- 동료평가 실행 순서를 문서 뒤쪽의 기능 체크리스트로 변경
- 현재 README 변경의 commit, push는 아직 수행하지 않음

## 현재 구조

- `Quiz`: 퀴즈 데이터, 형식 검증, 출력과 JSON 변환
- `QuizGame`: 메뉴, 입력, 퀴즈 진행, 추가·삭제와 점수 처리
- `StateManager`: 상태 파일 경로, JSON 저장·불러오기, 검증과 손상 복구
- `TimedTerminalInput`: 제한 시간, 카운트다운과 자동 힌트 입력
- `main.py`: 기본 객체 생성과 실행

기존 `QuizGame`에 있던 상태 경로 선택, JSON 직렬화, 스키마 검증, 이전 데이터
처리와 손상 파일 복구를 `src/state_manager.py`로 이동했다. `QuizGame`에는 현재 상태를
직접 전달하고 불러온 세 값을 반영하는 얇은 `save_state()`와 `load_state()`를
유지했다. 별도 상태 전달 클래스는 사용하지 않는다.

## 구현 완료

### 필수 기능

- 메뉴 1~6과 공통 정수·문자열·`y/n` 입력 검증
- 4지선다형 `Quiz` 클래스와 직접 작성한 기본 문제 5개
- 전체 상식 문제 중 풀이 수 선택과 무작위 출제
- 정답·오답·결과 출력과 단일 최고 점수 관리
- 퀴즈 추가, 목록, 삭제와 즉시 저장
- 프로젝트 루트 `state.json` 저장과 재실행 복원
- UTF-8 JSON 읽기·쓰기와 임시 파일 교체
- 파일 없음·문법 오류·스키마 오류 처리
- 손상 파일 백업 후 기본 데이터 복구
- 정상 종료, `Ctrl+C`, EOF 안전 종료

### 추가 기능

- 문제별 20초 제한 시간과 1초 카운트다운
- 10초 후 문제별 자동 힌트
- 힌트 전 3점, 힌트 후 1점 차등 점수
- 문제 수 선택과 중복 없는 무작위 출제
- 퀴즈 삭제와 저장 실패 롤백
- 전체 플레이 기록 저장과 최근 5개 표시

## 점수 정책

- 최고 점수는 실제 획득 점수끼리 비교한다.
- 문제 수에 따른 백분율이나 정규화 점수는 사용하지 않는다.
- 많은 문제를 선택할수록 획득 가능한 최대 점수가 높아지는 현재 동작을 유지한다.
- 이 정책은 추가 고려사항이 아니라 확정된 게임 규칙이다.

## 이번 리팩토링 검증

임시 디렉터리의 독립 상태 파일을 사용해 다음 동작을 확인했다.

- Python 구문과 import 성공
- `main.py`의 `StateManager`·`QuizGame` 객체 조립과 정상 종료
- EOF 입력 중단의 안전 종료
- 파일이 없을 때 기본 퀴즈 5개 저장과 재로드
- 새 퀴즈 추가 후 재로드
- 퀴즈 삭제 후 재로드
- 플레이 후 최고 점수와 히스토리 재로드
- 이전 카테고리별 점수 이전과 누락된 기본 힌트 복원
- 손상 JSON 백업과 기본 데이터 복구
- 손상 파일 백업 실패 후 원본 유지와 이후 저장 차단
- 저장 실패 시 퀴즈 추가 취소
- 저장 실패 시 퀴즈 삭제 취소
- 저장 실패 시 플레이 기록과 최고 점수 롤백
- README와 문서의 로컬 링크 확인
- `git diff --check` 통과와 추적 중인 `state.json` 무변경

이 검증은 코드 회귀 검사다. 미션의 최종 사용자 실행 증거는 macOS에서 별도로
확보한다.

## 기존 확보 증거

- 초기 Git 설정: `evidence/git/git-verification.md`, `evidence/git/git-log.png`
- 현재 메뉴: `evidence/screenshots/main-menu.png`
- 제한 시간·자동 힌트: `evidence/screenshots/game-play.png`
- 안전 종료: `evidence/logs/safe-exit.md`
- 최고 점수 재실행: `evidence/logs/persistence-restart.md`
- 손상 JSON 복구: `evidence/logs/json-recovery.md`
- 브랜치 병합 이력: `feature/solving`, `state-json`, `feature/bonus`,
  `Test/time-limit`
- 의미 있는 커밋 10개 이상 확보

## 남은 필수 확인

- macOS zsh에서 README 동료평가 순서 최종 실행
- 현재 구조에서 추가 퀴즈와 점수의 종료·재실행 영속성 원본 증거
- 추가·목록·플레이 결과·점수 화면 최종 확보
- 최종 Git 그래프 확보
- 별도 디렉터리 clone·push 후 기존 작업 폴더 pull 반영 증거
- 전체 제출물 비밀값 최종 점검

## 다음 작업

현재 README 문서 변경을 커밋한 뒤, 동료평가 체크리스트에 따라 macOS에서 최종 실행
증거를 확보한다.
