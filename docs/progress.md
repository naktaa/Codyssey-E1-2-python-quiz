# 진행 상태

## 현재 단계

- 브랜치: `main`
- 최신 기능·문서 커밋:
  `934811e Refactor: 메뉴·점수 처리와 상태 저장 형식 개선`
- 최종 점검 시 `main`과 `origin/main`은 `934811e`에서 동기화되어 있었다.
- 현재 작업: 클래스명·증거 채택 기준·최종 판정을 문서 전체에 동기화
- 상태: 필수 기능, 산출물과 증거 확인 완료
- 테스트 상태는 `python3 main.py --test`로 선택
- 플레이 기록은 플레이 시각과 점수만 저장

## 현재 구조

- `Quiz`: 퀴즈 데이터, 형식 검증, 출력과 JSON 변환
- `GameManager`: 메뉴, 입력, 퀴즈 진행, 추가·삭제와 점수 처리
- `StateManager`: JSON 저장·불러오기, 검증과 손상 복구
- `TimedTerminalInput`: 제한 시간, 카운트다운과 자동 힌트 입력
- `main.py`: 기본 객체 생성과 실행

`main.py`가 실행 옵션에 맞는 상태 경로를 선택하고 `StateManager`를 생성한다.
`GameManager`에는 생성된 `StateManager`만 전달한다. `GameManager`의 `save_state()`와
`load_state()`는 현재 상태 전달과 반영을 담당하고 실제 파일 처리는 하지 않는다.
기본 퀴즈는 정상 상태 파일을 읽을 때 만들지 않고 파일이 없거나 손상됐을 때만
`StateManager._create_default_state()`에서 생성한다.

## 구현 완료

### 필수 기능

- 메뉴 1~6과 공통 정수·문자열·`y/n` 입력 검증
- 4지선다형 `Quiz` 클래스와 직접 작성한 기본 문제 5개
- 전체 상식 문제 중 풀이 수 선택과 무작위 출제
- 정답·오답·결과 출력과 단일 최고 점수 관리
- 퀴즈 추가, 목록, 삭제와 즉시 저장
- 프로젝트 루트 `state.json` 저장과 재실행 복원
- UTF-8 JSON 읽기·쓰기와 임시 파일 교체
- 파일 없음·문법 오류·스키마 오류 처리
- 손상 파일 백업 후 기본 데이터 복구
- 정상 종료, `Ctrl+C`, EOF 안전 종료

### 추가 기능

- 문제별 10초 제한 시간과 1초 카운트다운
- 5초 후 문제별 자동 힌트
- 정답 3점에서 힌트 사용 시 2점을 차감하는 점수
- 정답이면 점수를 즉시 반영하고 각 문제 결과 뒤에 현재 누적 점수 표시
- 메뉴 번호와 실행 메서드를 딕셔너리로 연결한 디스패치
- 메뉴·기능별 공통 구분선과 문제 진행 수 표시
- 문제 수 선택과 중복 없는 무작위 출제
- 퀴즈 삭제와 저장 실패 롤백
- 전체 플레이 기록 저장과 최근 5개 표시
- 실제 상태와 분리된 `--test` 실행 옵션
- JSON 4칸 들여쓰기와 초 단위 손상 백업 파일명

## 점수 정책

- 최고 점수는 실제 획득 점수끼리 비교한다.
- 문제 수에 따른 백분율이나 정규화 점수는 사용하지 않는다.
- 많은 문제를 선택할수록 획득 가능한 최대 점수가 높아지는 현재 동작을 유지한다.
- 이 정책은 추가 고려사항이 아니라 확정된 게임 규칙이다.

## 최신 화면·시간·점수 검증

- macOS / zsh / Python 3.12.13에서 Python 구문과 JSON 형식 확인
- 메뉴와 기능별 공통 구분선, 등록 문제 수와 최고 점수 표시 확인
- `[문제 현재/전체]` 진행 수와 별도 최종 결과 구역 확인
- 문제별 10초 카운트다운과 5초 후 힌트 줄 제자리 갱신 확인
- 힌트 전 3점, 힌트 후 `3점 - 2점 = 1점`, 오답·시간 초과 0점 확인
- 각 문제 뒤 `현재 점수` 3점·1점·0점·0점 출력 회귀 검사 완료
- `힌트 사용 횟수`와 `최종 점수` 출력 및 최종 점수 저장 회귀 검사 완료
- 시간 초과 후 메뉴 복귀와 Ctrl+C 안전 종료 확인
- 기존 10초·5초 기능은 `9d76c98`로 commit·push 완료
- 사용자가 `py --test`로 macOS 터미널의 메뉴·추가·목록·플레이·점수·
  삭제·종료 흐름을 직접 실행했다.
- 긴 터미널 과정은 `evidence/logs/final-game-verification.md`에 기능별로 정리했다.
- 직접 실행 후 변경된 `현재 점수` 표시는
  `evidence/logs/current-score-regression.md`의 3점·1점·0점·0점 회귀 검사로 보완했다.
- 사용자 결정에 따라 위 두 기록을 함께 최종 기능 증거로 채택하며,
  별도의 최신 터미널 캡처는 추가하지 않는다.

## 구조 단순화 검증

- 정상 상태 로드 시 기본 퀴즈를 생성하지 않음
- 파일 없음과 손상 복구 시에만 기본 퀴즈를 한 번씩 생성
- `GameManager` 생성과 실제·테스트 상태 로드
- Python 파일 7개 구문 검사
- `python3 main.py --help`의 `--test` 옵션 확인
- `python3 main.py --test`로 테스트 상태 생성과 정상 종료
- 테스트 상태에서 퀴즈 추가·목록 확인·삭제 후 기본 상태 복원
- 최근 점수 6건 저장 후 마지막 5건 역순 표시
- 최고 점수와 최근 플레이 기록 저장 후 재로드
- 마지막 퀴즈 삭제 후 과거 최고 점수와 기록 유지
- 점수 저장 실패 시 최고 점수와 새 기록 rollback
- 이전 `best_scores`와 힌트가 누락된 퀴즈 데이터 거부
- `git diff --check` 통과와 추적 중인 `state.json` 무변경

## 이전 리팩토링 검증

임시 디렉터리의 독립 상태 파일을 사용해 다음 동작을 확인했다.

- Python 구문과 import 성공
- `main.py`의 `StateManager`·`GameManager` 객체 조립과 정상 종료
- EOF 입력 중단의 안전 종료
- 파일이 없을 때 기본 퀴즈 5개 저장과 재로드
- 새 퀴즈 추가 후 재로드
- 퀴즈 삭제 후 재로드
- 플레이 후 최고 점수와 히스토리 재로드
- 손상 JSON 백업과 기본 데이터 복구
- 손상 파일 백업 실패 후 원본 유지와 이후 저장 차단
- 저장 실패 시 퀴즈 추가 취소
- 저장 실패 시 퀴즈 삭제 취소
- 저장 실패 시 플레이 기록과 최고 점수 롤백
- README와 문서의 로컬 링크 확인
- `git diff --check` 통과와 추적 중인 `state.json` 무변경

이 검증은 코드 회귀 검사다. 미션의 최종 사용자 실행 증거는 macOS에서 별도로
확보한다.

## 기존 확보 증거

- 초기 Git 설정: `evidence/git/git-verification.md`, `evidence/git/git-log.png`
- 개선 전 20초 제한 시간·10초 자동 힌트:
  `evidence/screenshots/game-play.png`
- 안전 종료: `evidence/logs/safe-exit.md`
- 최고 점수 재실행: `evidence/logs/persistence-restart.md`
- 손상 JSON 복구: `evidence/logs/json-recovery.md`
- 화면 변경 전 메뉴·추가·목록·10초·5초 플레이·점수·삭제·종료:
  `evidence/logs/final-game-verification.md`
- 최신 문제별 현재 점수 3점·1점·0점·0점:
  `evidence/logs/current-score-regression.md`
- 기능 브랜치 분기·병합 그래프:
  `evidence/screenshots/git-log.png`
- 기존 두 작업 환경의 clone·push·pull reflog:
  `evidence/logs/clone-pull.md`
- 브랜치 병합 이력: `feature/solving`, `state-json`, `feature/bonus`,
  `Test/time-limit`
- 의미 있는 커밋 10개 이상 확보

## 최종 판정

- 최종 게임 진행 클래스명은 `GameManager`로 통일한다.
- macOS 직접 실행 기록과 최종 코드 회귀 검사를 함께 기능 증거로 채택한다.
- 기존 VSCode Git 그래프를 브랜치 분기·병합 증거로 채택한다.
- 재실행 영속성은 기존 점수 재실행 기록과 최종 격리 저장·재로드 검사로
  최종 확인했다.
- 추가 터미널 캡처나 Git 그래프 갱신은 요구하지 않는다.

## 다음 작업

이 문서 동기화 변경을 검토한 뒤, 사용자가 원할 때 commit·push한다.
