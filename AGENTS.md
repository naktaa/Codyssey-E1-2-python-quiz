# AGENTS.md

## 목적

이 문서는 Codex가 E1-2 퀴즈 게임 미션을 단계별로 안전하게 수행하기 위한 작업 지침이다.

## 작업 전 필수 확인

작업을 시작하기 전에 다음 문서를 순서대로 읽는다.

1. `MISSION.md`
2. `docs/requirements.md`
3. `docs/progress.md`
4. 필요 시 `docs/worklog.md`, `docs/troubleshooting.md`

문서보다 **현재 코드, 실제 실행 결과, Git 상태**를 우선한다. 문서와 실제 상태가 다르면 임의로 맞추지 말고 차이를 보고한다.

## 기본 원칙

- 전체 미션을 한 번에 구현하지 않는다.
- 한 번에 하나의 논리적 단계만 처리한다.
- 기능 검증은 사용자가 `main.py`를 직접 실행한 원본 로그나 캡처를 우선한다.
- 필수 요구사항을 먼저 완료하고 보너스 과제는 사용자 승인 후 진행한다.
- 실행하지 않은 결과, 로그, Git 이력, 스크린샷을 만들지 않는다.
- 확인하지 않은 항목은 `예정`, `미검증`, `확인 필요`로 표시한다.
- commit, push, merge는 사용자가 요청한 경우에만 수행한다.
- 기존 사용자의 변경사항을 임의로 삭제하거나 덮어쓰지 않는다.
- 관련 없는 파일을 수정하지 않는다.
- 비밀번호, 토큰, API 키, SSH 개인키, 인증 코드, 민감한 환경 변수 값을 저장하지 않는다.
- 기본 개발·검증 환경은 교육장 macOS와 zsh로 간주한다.
- Windows 검증은 별도 요구가 있을 때만 수행한다.
- 개인 PC의 절대 경로를 코드에 넣지 않는다.
- Python 파일은 UTF-8, 줄바꿈은 LF, 파일명은 영문 소문자를 우선한다.
- 외부 라이브러리를 추가하지 않는다. Python 표준 라이브러리만 사용한다.

## 구현 구조 원칙

- 최소 `Quiz`, `QuizGame` 두 클래스를 분리한다.
- 입력 처리, 게임 진행, 데이터 저장·불러오기 책임을 메서드로 분리한다.
- 데이터 파일 경로는 실행 위치에 의존하지 않도록 프로젝트 루트를 기준으로 계산한다.
- 최종 데이터 파일은 프로젝트 루트의 `state.json`이어야 한다.
- 숫자 입력은 공백 제거, 빈 입력, 숫자 변환 실패, 범위 밖 입력을 모두 처리한다.
- `KeyboardInterrupt`, `EOFError` 발생 시 가능한 범위에서 저장 후 안내 메시지와 함께 종료한다.
- `state.json`이 없거나 손상된 경우 기본 퀴즈로 실행할 수 있어야 한다.
- 손상된 데이터를 복구할 때 원본을 임의 삭제하기보다 안전한 처리 방법을 먼저 검토한다.

## `작업 시작` 입력 시

1. 저장소 루트를 확인한다.
2. `git status --short --branch`로 브랜치와 작업 트리를 확인한다.
3. `git log --oneline --decorate -5`로 최근 커밋을 확인한다.
4. 미션·요구사항·진행 문서를 읽는다.
5. 실제 코드와 문서 상태를 비교한다.
6. 완료, 미검증, 미해결 상태를 요약한다.
7. 아래 우선순위에 따라 다음 작업 하나를 선정한다.
8. 수정 파일, 실행 명령, 정상 기준, 증거, 권장 커밋 메시지를 안내한다.
9. 위험하거나 모호한 부분이 없으면 해당 단계만 진행한다.

### 작업 우선순위

1. 커밋되지 않은 작업 마무리
2. 미해결 오류 해결
3. 현재 브랜치 작업 완료
4. 다음 미완료 필수 요구사항
5. 전체 검증과 증거 수집
6. README 반영과 최종 검수
7. 보너스 과제

## `상태 확인` 입력 시

파일을 수정하지 않고 다음을 보고한다.

- 현재 브랜치와 변경사항
- 최근 커밋
- 완료·미검증 요구사항
- 확보·누락된 증거
- 미해결 문제
- 코드와 문서의 불일치
- 다음 권장 작업 하나

## `작업 종료` 입력 시

- 변경사항을 실제 명령으로 검증한다.
- `docs/requirements.md` 상태를 실제 결과에 맞게 갱신한다.
- `docs/progress.md`에 현재 단계와 다음 작업을 기록한다.
- `docs/worklog.md`에 실행 명령과 실제 결과를 기록한다.
- 실제 오류를 해결했다면 `docs/troubleshooting.md`에 기록한다.
- 검증된 내용만 `README.md`에 반영한다.
- 필요한 스크린샷의 명령, 캡처 범위, 파일명을 안내한다.
- Git 상태와 변경 파일을 요약한다.
- 권장 커밋 메시지와 다음 작업을 기록한다.

## Git 작업 원칙

- 저장소 이름 권장: `codyssey-e1-2-quiz-game`
- 기본 브랜치: `main`
- 퀴즈 풀기 기능 브랜치 권장: `feature/quiz-play`
- 브랜치 요구사항을 충족하기 위해 실제로 `checkout`, 작업, commit, `merge` 과정을 수행한다.
- 커밋 수를 채우기 위한 빈 파일 수정이나 의미 없는 커밋을 만들지 않는다.
- 권장 커밋 접두사:
  - `Chore: 초기 설정`
  - `Feat: 기능 구현`
  - `Fix: 오류 수정`
  - `Refactor: 구조 개선`
  - `Docs: 문서 작성`
  - `Test: 테스트 및 검증`
- clone·pull 실습은 개발 완료 후 별도 디렉터리에서 수행한다.
- `git reset --hard`, `git clean -fd`, `git push --force` 등 이력이나 파일을 잃을 수 있는 명령은 명시적 요청 없이 사용하지 않는다.

## 증거 원칙

실제 증거는 `evidence/`에 저장하고 문서 파일과 섞지 않는다.

권장 구조:

```text
evidence/
├── screenshots/
│   ├── env-python-git.png
│   ├── menu.png
│   ├── add-quiz.png
│   ├── quiz-list.png
│   ├── play-result.png
│   ├── best-score.png
│   ├── persistence-restart.png
│   ├── git-graph.png
│   └── clone-pull.png
└── logs/
    └── final-verification.txt
```

- 실행 전에는 증거 파일을 만들지 않는다.
- 화면에 계정명, 이메일, 토큰, 절대 경로가 불필요하게 노출되지 않도록 확인한다.
- 텍스트로 충분히 증명되는 내용은 `evidence/logs/`에 저장할 수 있다.
